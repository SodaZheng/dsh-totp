# DSH 内嵌界面约定

用途：已有 DSH 界面中的访问验证设置、扫码流程和登录入口；遵循产品现有视觉规范。

- 颜色权威：安装的 `@deepseek-ai/dsh-client-ui-theme@0.1.2-rc.1` 的 `--dsw-*` 变量；[官方主题说明](https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/client/ui-theme/README.md)。
- 浅色：底色 `#fff`，正文 `#0f1115`，次级文字 `#61666b`；主按钮黑底白字。
- 深色：底色 `#151517`，弹层 `#2c2c2e`，正文 `#f9fafb`，次级文字 `#cfd3d6`；主按钮白底深色字。
- 内嵌面板直接继承 `--dsw-alias-bg-layer-2`、`--dsw-alias-label-primary`、`--dsw-alias-label-secondary`、`--dsw-alias-border-l2` 和按钮语义变量，响应主题即时切换。
- 登录页在 DSH 尚未加载前使用相同的内置色值，读取 Host 的 `ui-theme.preference`；随后以系统偏好解析 system 模式。
- 字体使用 DSH 的 `--dsw-font-family`，正文 14px，页面标题 26px、状态标题 20px；保持工具界面的紧凑信息密度。
- 控制页按“保护状态 → 验证方式与恢复 → 锁定动作”组织；用原生灰阶表面、细描边和微弱光感区分层次。
- 验证码确认区按操作展开，避免常驻输入框和重复按钮；绑定流程标明身份验证、扫码、保存恢复码三个阶段。
- 恢复码以原始字符值展示并支持复制，视觉换行不改变凭据内容；不添加虚构设备数量或统计。
- 间距 8px 倍数，按钮圆角 10px、面板 20px，轻边框和现有提升阴影；只做短时 hover 反馈。
- QR 图像为真实 TOTP 数据生成的二维码，固定白底确保扫描对比度；不重绘 DSH 标志，应用内沿用 DSH 已有品牌界面。
- 不增加新的主题选择器、配色变体或 Tweaks 面板。
- 所有六位动态码（登录、首次绑定、换绑前验证、新验证器确认、保护开关、更新恢复码、恢复后重新绑定）共用 `otp-form` / `input.code`：最大 420px 的居中输入区与等宽主按钮、76px 高度、34px 等宽数字、`000000` 占位及统一聚焦效果；表单窄于 340px 时缩至 68px / 30px，较矮窗口统一缩至 68px 高度。保留单一原生输入框，支持整组粘贴及自动填充。扫码阶段在其上方展示居中二维码和分隔线。
